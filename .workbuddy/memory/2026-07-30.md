# 2026-07-30

- 核查每日盈利快照功能：提交 a326002 已实现推送成功后调用 recordDailySnapshot，将刷新后的总资产按日期写入 Config/settings.json 的 portfolio.profitHistory；同日覆盖、排序并保留 366 条，现金不进入资产明细。完整 npm test 通过。当前 master 比 origin/master 超前 1 个提交，需推送后 GitHub Actions 才会实际开始按工作日积累数据点；现有历史共 7 点，不会自动回填过去缺失日期。
- 修复 13 个资产全部刷新失败：实测 push2.eastmoney.com 会被远端断开（UND_ERR_SOCKET），导致浏览器 fetch 后 JSONP 也失败；备用 fundmobapi 本身可用，13 个代码连续三轮均成功。网页与推送端实时行情域名改为 push2delay.eastmoney.com，并增加两端回归断言。真实完整主备链路验证 13 成功、0 失败，完整 npm test 通过。改动尚未提交/推送。

